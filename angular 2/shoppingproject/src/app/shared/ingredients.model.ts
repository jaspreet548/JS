export class Ingredient {
    constructor(public name: string, public amount: number) {}
} // this is shortcut method provided by
    // typescript to create property and intilize it by adding just public
